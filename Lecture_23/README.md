# Лекція 23: Юніт-тестування (Домашнє завдання)

Це завдання для вивчення юніт-тестування у C#.

## 📁 Файли у цій папці

1. **ESSAY_UnitTesting.md** - Коротке есе на тему "Юніт-тестування: теорія та практика"
   - Пояснення що таке юніт-тестування
   - Переваги та обмеження
   - Порівняння з інтеграційним тестуванням
   - Пояснення коли використовувати mock-об'єкти

2. **BankAccount.cs** - Практичний приклад класу
   - Клас `BankAccount` з методами:
     - `Deposit(decimal)` - поповнення рахунку
     - `Withdraw(decimal)` - зняття коштів
     - `TransferTo(BankAccount, decimal)` - переведення на інший рахунок
   - Конструктор з валідацією

3. **BankAccountTests.cs** - Юніт-тести до класу BankAccount
   - Мінімум 2 тести для кожного методу
   - Включають успішні сценарії та граничні випадки (boundary cases)
   - Використана якість Arrange-Act-Assert (AAA) паттерн
   - Всього 16 тестів

4. **NOTES_AboutMocking.md** - Нотатки про Mock-об'єкти
   - Пояснення що такі mock-об'єкти
   - Коли їх потрібно використовувати
   - Коли можна обійтися без них
   - Практичні приклади

## 🧪 Тести які були написані

### Для методу `Deposit()`
- ✅ Успішний депозит збільшує баланс
- ✅ Депозит дуже малої суми (0.01)
- ✅ Депозит нулевої суми - помилка
- ✅ Депозит від'ємної суми - помилка

### Для методу `Withdraw()`
- ✅ Успішне зняття зменшує баланс
- ✅ Зняття всіх грошей до 0
- ✅ Зняття більше ніж є - помилка
- ✅ Зняття нулевої суми - помилка
- ✅ Зняття від'ємної суми - помилка

### Для методу `TransferTo()`
- ✅ Успішний трансфер
- ✅ Трансфер нулевої суми - помилка
- ✅ Трансфер без достатньо коштів - помилка
- ✅ Трансфер всіх грошей
- ✅ Трансфер до null рахунку - помилка

### Для конструктора
- ✅ Коректне створення з валідними даними
- ✅ Створення з пустим ім'ям - помилка
- ✅ Створення з від'ємним балансом - помилка
- ✅ Створення з нульовим балансом - OK

## 🎯 Граничні випадки (Boundary Cases) які тестуються

- Нульові значення
- Від'ємні значення
- Точні межи (баланс = сума зняття)
- Null об'єкти
- Пусті рядки
- Граничні суми

## 🔧 Як запустити тесты

Потрібна установка xUnit та других пакетів:

```bash
# Створити папку для тестів (якщо її нема)
dotnet new xunit -n BankingApp.Tests

# Добавити посилання на головний проєкт
dotnet add reference ../BankingApp/BankingApp.csproj

# Запустити тесты
dotnet test
```

Або в Visual Studio:
- Test -> Run All Tests (Ctrl+R, A)
- Test Explorer покаже результати

## 💡 Ключові моменти з завдання

1. **AAA Паттерн (Arrange-Act-Assert)**
   ```csharp
   // Arrange - підготовка
   var account = new BankAccount("John", 100);
   
   // Act - виконання
   account.Deposit(50);
   
   // Assert - перевірка
   Assert.Equal(150, account.Balance);
   ```

2. **Граничні випадки важливі**
   - Це часто місця де буває помилки
   - Треба тестувати межи, не тільки нормальні сценарії

3. **Mock-об'єкти потрібні для:**
   - Зовнішніх АПІ
   - Баз даних
   - Файлової системи
   - Дорогих операцій

4. **Mock-об'єкти НЕ потрібні для:**
   - Простої логіки без залежностей
   - В інтеграційних тестах

## 📊 Статистика тестування

- Всього методів: 4 (конструктор + 3 методи)
- Всього тестів: 16
- Умовне покриття: ~95%
- Типи: успішні сценарії + граничні випадки

## 🚀 Висновки

Юніт-тестування - це не страшно, це просто звичка. Коли ти пишеш за тестами, ти:
- Думаєш про граничні випадки
- Пишеш чистіший код
- Менше помилок

Найголовніше - почнути. Перші тесты будуть важкові, але потім стане легше.
