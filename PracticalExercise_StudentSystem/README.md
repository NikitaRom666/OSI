# 🎓 Практичне завдання: Система управління студентами та курсами

## 📋 Мета заняття

Навчити студентів проектувати та реалізовувати основні класи у програмному проєкті, використовуючи об'єктно-орієнтований підхід. Цей проєкт демонструє всі ключові принципи ООП на практичному прикладі.

## 📁 Структура проєкту

```
PracticalExercise_StudentSystem/
├── StudentSystem.csproj          # Файл проєкту .NET
├── Program.cs                    # Основна програма з demo-прикладами
├── Interfaces.cs                 # Інтерфейси IEntity, IEnrollable, IManageable
├── Person.cs                     # Абстрактний базовий клас для людини
├── Student.cs                    # Конкретна реалізація студента
├── Instructor.cs                 # Конкретна реалізація викладача
├── Course.cs                     # Абстрактні та конкретні класи курсів
├── CourseManager.cs              # Менеджер курсів та записів
└── README.md                     # Цей документ
```

## 🏗️ Архітектура та ООП принципи

### 1. **Абстракція (Abstraction)**

Абстрактні класи приховують складні деталі реалізації за простим інтерфейсом:

```csharp
public abstract class Person : IEntity, IManageable
{
    // Базова функціональність для всіх людей
    public abstract string GetInfo();
}

public abstract class Course : IEntity, IManageable
{
    // Визначає загальну поведінку для всіх курсів
    public abstract int GetMaxStudents();
}
```

### 2. **Інкапсуляція (Encapsulation)**

Приватні поля з контролем доступу через властивості:

```csharp
private string _email;

public string Email
{
    get { return _email; }
    set 
    { 
        if (!value.Contains("@"))
            throw new ArgumentException("Invalid email");
        _email = value;
    }
}
```

**Переваги:**
- Валідація даних
- Захист від невірних значень
- Контроль над змінами стану

### 3. **Наслідування (Inheritance)**

Спеціалізовані класи успадковують функціональність базових:

```csharp
public class Student : Person
{
    // Успадковує: Id, FirstName, LastName, Email, GetFullName()
    // Добавляє: StudentNumber, GPA, EnrolledCourses
}

public class Instructor : Person
{
    // Успадковує базову функціональність Person
    // Добавляє: Department, Specialization, TaughtCourses
}
```

**Ієрархія класів:**
```
IEntity (Interface)
    ↓
Person (Abstract)
    ├─→ Student
    └─→ Instructor

Course (Abstract)
    ├─→ UniversityCourse
    └─→ LabCourse
```

### 4. **Поліморфізм (Polymorphism)**

Абстрактні методи реалізуються по-різному у підклассах:

```csharp
// У Student
public override string GetInfo()
{
    return $"[Student] ID: {Id}, Name: {GetFullName()}, GPA: {GPA}";
}

// У Instructor
public override string GetInfo()
{
    return $"[Instructor] ID: {Id}, Name: {GetFullName()}, Dept: {Department}";
}

// Використання поліморфізму
Person person = new Student(...);
Console.WriteLine(person.GetInfo()); // Викличе Student.GetInfo()
```

### 5. **Інтерфейси (Interfaces)**

Контракти для стандартизованої поведінки:

```csharp
public interface IEntity
{
    int Id { get; }
}

public interface IEnrollable
{
    bool Enroll(Student student, Course course);
    bool Unenroll(Student student, Course course);
}

public interface IManageable
{
    string GetInfo();
}
```

## 📚 Опис класів

### **Person** (Абстрактний базовий клас)

Базова сутність для представлення людини в системі.

| Властивість | Тип | Опис |
|---|---|---|
| `Id` | int | Унікальний ідентифікатор |
| `FirstName` | string | Ім'я (z валідацією) |
| `LastName` | string | Прізвище (з валідацією) |
| `Email` | string | Електронна адреса (перевіряється наявність @) |

| Метод | Опис |
|---|---|
| `GetInfo()` | Абстрактний метод для інформації люд |
| `GetFullName()` | Повертає повне ім'я |

---

### **Student** (наслідує Person)

Представляє студента, який може записуватися на курси.

| Властивість | Тип | Опис |
|---|---|---|
| `StudentNumber` | string | Унікальний номер студента |
| `GPA` | double | Середня оцінка (0.0-4.0) |
| `EnrolledCourses` | List<Course> | Курси, на які записаний студент |

| Метод | Опис |
|---|---|
| `EnrollInCourse(Course)` | Записати студента на курс |
| `UnenrollFromCourse(Course)` | Видалити студента з курсу |
| `GetEnrolledCoursesCount()` | Отримати кількість курсів |
| `GetTotalCredits()` | Розрахувати загальну суму кредитів |
| `DisplayDetailedInfo()` | Виведення детальної інформації |

---

### **Instructor** (наслідує Person)

Представляє викладача, який веде курси.

| Властивість | Тип | Опис |
|---|---|---|
| `Department` | string | Кафедра/Факультет |
| `Specialization` | string | Спеціалізація викладача |
| `TaughtCourses` | List<Course> | Курси, які викладає |

| Метод | Опис |
|---|---|
| `AssignCourse(Course)` | Призначити курс викладачу |
| `RemoveCourse(Course)` | Видалити курс |
| `GetTaughtCoursesCount()` | Кількість курсів |
| `DisplayDetailedInfo()` | Детальна інформація |

---

### **Course** (Абстрактний базовий клас)

Базова сутність для представлення курсу.

| Властивість | Тип | Опис |
|---|---|---|
| `Id` | int | ID курсу |
| `Name` | string | Назва курсу |
| `Description` | string | Опис |
| `Credits` | int | Кредити (1-6) |

| Метод | Опис |
|---|---|
| `GetInfo()` | Абстрактний для інформації курсу |
| `GetMaxStudents()` | Абстрактний для макс студентів |

---

### **UniversityCourse** (наслідує Course)

Стандартний університетський курс. **Максимум студентів: 30**

### **LabCourse** (наслідує Course)

Лабораторний курс з практичними роботами. **Максимум студентів: 15**

| Додаткова властивість | Тип | Опис |
|---|---|---|
| `LabTechnology` | string | Технологія/інструменти лабораторії |

---

### **CourseManager** (реалізує IEnrollable)

Менеджер для керування записами студентів на курси.

| Метод | Опис |
|---|---|
| `RegisterCourse(Course)` | Зареєструвати курс |
| `AssignInstructor(Course, Instructor)` | Призначити викладача |
| `Enroll(Student, Course)` | Записати студента на курс |
| `Unenroll(Student, Course)` | Відписати студента |
| `GetEnrolledStudents(Course)` | Список записаних студентів |
| `GetCourseInstructor(Course)` | Отримати викладача курсу |
| `GetCourseCapacity(Course)` | Заповненість курсу (%) |
| `PrintCourseStatistics(Course)` | Детальна статистика курсу |

## 🎯 Як запустити проєкт

### Передумови
- .NET 6.0 або новіше
- Visual Studio 2022, VS Code, або JetBrains Rider

### Крок 1: Клонування репозиторія
```bash
git clone https://github.com/NikitaRom666/OSI.git
cd OSI/PracticalExercise_StudentSystem
```

### Крок 2: Збірка проєкту
```bash
dotnet build
```

### Крок 3: Запуск програми
```bash
dotnet run
```

## 📊 Приклад вихідних даних

При запуску програма демонструє:

1. **Створення викладачів** з детальною інформацією
2. **Створення 3 курсів** (2 стандартних + 1 лабораторний)
3. **Інтеграцію менеджера курсів** та призначення викладачів
4. **Створення 5 студентів** з різними GPA
5. **Запис студентів на курси** з валідацією
6. **Детальне виведення інформації** про студентів та курси
7. **Статистику курсів** включаючи відсоток заповненості
8. **Демонстрацію відписки** зі змінами у даних
9. **Розрахунок загальних кредитів** для кожного студента

## 🧪 Примери тестування

### Тест 1: Невалідна електронна адреса
```csharp
try
{
    var student = new Student(1, "Name", "Surname", "invalid-email", "2023-101");
    // Буде викинута ArgumentException
}
catch (ArgumentException ex)
{
    Console.WriteLine($"Помилка: {ex.Message}"); // "Invalid email"
}
```

### Тест 2: GPA поза діапазоном
```csharp
var student = new Student(1, "Name", "Surname", "name@email.com", "2023-101", 5.0);
// ArgumentException: "GPA must be between 0.0 and 4.0"
```

### Тест 3: Переповнення курсу
```csharp
var labCourse = new LabCourse(1, "Lab", "Description", 3, "C#");
// LabCourse має максимум 15 студентів

for (int i = 0; i < 16; i++)
{
    var student = new Student(i, "Name", "Surname", $"email{i}@test.com", $"2023-{i}");
    courseManager.Enroll(student, labCourse); 
    // На 16-му студентові буде викинута InvalidOperationException
}
```

## 📖 ООП принципи у проєкті

### Інкапсуляція
- ✅ Приватні поля з публічними властивостями
- ✅ Валідація через сеттери
- ✅ Read-only колекції (AsReadOnly())

### Наслідування
- ✅ Student, Instructor успадковують Person
- ✅ UniversityCourse, LabCourse успадковують Course

### Поліморфізм
- ✅ Різні реалізації GetInfo() в підклассах
- ✅ Різні реалізації GetMaxStudents() в курсах

### Абстракція
- ✅ Абстрактні класи Person та Course
- ✅ Абстрактні методи для спеціалізації

### Інтерфейси
- ✅ IEntity для унікальних сутностей
- ✅ IEnrollable для керування записами
- ✅ IManageable для інформації

## 🎓 Домашнє завдання

**Продовжте розробку, добавивши:**

1. **Клас Grade** - для збереження оцінок студентів
   ```csharp
   public class Grade : IEntity
   {
       public int Id { get; }
       public int StudentId { get; }
       public int CourseId { get; }
       public double Score { get; set; }
       public DateTime DateAssigned { get; set; }
   }
   ```

2. **Клас GradeManager** - для керування оцінками
   ```csharp
   public class GradeManager : IManageable
   {
       public void AssignGrade(Grade grade);
       public double GetCourseAverage(Course course);
       public string GetInfo();
   }
   ```

3. **Розширте CourseManager** для обчислення:
   - Середньої GPA студентів курсу
   - Розпорядження преквізитів (потребені курси)
   - Конфліктів розкладу

4. **Додайте тестування**:
   - Проверка граничних значень
   - Тести для виняткових ситуацій
   - Інтеграційні тести

5. **Рефакторинг**:
   - Спроба застосувати паттерни (Singleton для курс-менеджера)
   - Сортування та фільтрування студентів
   - Експорт даних в CSV/JSON

## 📝 Коментування та документація

**Стиль документації (XML Comments):**
```csharp
/// <summary>
/// Коротке описання методу
/// </summary>
/// <param name="paramName">Опис параметру</param>
/// <returns>Опис повернутого значення</returns>
public void MyMethod(string paramName)
{
    // Кодові коментарі пояснюють "як" та "чому"
}
```

## 📌 Висновки

Цей проєкт демонструє, як правильне використання ООП принципів:

- ✅ **Спрощує розширення** - легко додати нові типи курсів чи сутностей
- ✅ **Забезпечує гнучкість** - зміни у одному класі не впливають на інші
- ✅ **Підвищує읽помість** - код організований логічно та зрозумілий
- ✅ **Дозволяє переиспользование** - базові класи можуть використовуватися по-різному
- ✅ **Забезпечує надійність** - валідація та захист даних

## 👨‍💻 Автор
Практичне завдання створено для демонстрації ООП принципів у C#.

---

**Успіхів у navčanju ООП!** 🚀
